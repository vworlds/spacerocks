import { Relationship } from '@vworlds/vecs';

/**
 * Indicates that an entity's Health is displayed through a ProgressBar on the
 * relationship's owned target.
 */
export class HealthBar extends Relationship {}
