# SR-013: Rehome Tests and Add Authoritative Server Coverage

## Objective

Move existing gameplay coverage from client-only tests to server-authoritative tests and add integration coverage for vecs networking behavior.

## Scope

- Move or duplicate DOM-free gameplay tests into `apps/server/tests`.
- Add tests for migrated movement, wrapping, spawning, shooting, collision, health, score, and ownership cleanup.
- Add tests for network component registration order.
- Add client render tests with mocked mirrored entities.
- Update old client tests that assumed gameplay authority in the browser.

## Dependencies

- `SR-005`

## Blocks

- `SR-014`

## Test Areas

- Network schema order and `Position` type id.
- Player session creates exactly one ship.
- Disconnect destroys child-owned entities.
- Input changes only owning ship.
- Movement and wrapping match expected behavior.
- Shooting respects cooldown/ammo.
- Collision changes health/score/entities correctly.
- Waves spawn after clear.
- Pickup effects apply correctly.
- Explosion marker TTL expires.

## Implementation Notes

- Prefer direct `ServerWorld` tests for gameplay systems instead of full WebRTC tests where possible.
- Keep a small number of integration tests for client/server connection if test infrastructure supports it.
- Tests should assert that client-only systems do not mutate authoritative gameplay state.

## Acceptance Criteria

- `npm test` passes with server-authoritative gameplay tests.
- Old tests are either migrated, rewritten, or intentionally removed with equivalent coverage.
- `npm run typecheck` and `npm run lint` pass.

## Review

**Verdict:** approved

**Verification gates:**
- typecheck: pass (all three workspaces — client, server, common)
- lint: pass (all three workspaces)
- test: pass (client 69/69, server 25/25, common 4/4)
- build: n/a

**Must-fix:**
- (none)

**Nits (optional):**
- `setActiveWeapon` now sets `WeaponView` directly and calls `player.modified(WeaponView)`, but the collision handler at `combat.ts:278` also calls `syncWeaponView(player)` after `setActiveWeapon`, resulting in a redundant second sync. Harmless, could be cleaned up in SR-014.

**Notes:**
- All 10 test areas from the ticket are covered by server tests (movement, wrapping, spawning, shooting, collision, health, score, wave progression, pickup effects, explosion TTL, schema order, session/ship ownership, disconnect cleanup, input isolation, cooldown/ammo enforcement).
- The combat.ts fix is correct and minimal: stores shield component locally to avoid re-reading after `set`, and populates `WeaponView` in `setActiveWeapon` with proper `modified()` calls so pickup weapon changes sync to clients.
- Removed client gameplay tests are replaced by server-authoritative equivalents; remaining client tests (DecaySystem, Render, UI, draw/*) are render/UI tests that do not assert gameplay authority.
- No production code in `apps/client/src` (systems or factories) was removed — diff is limited to test files and the targeted combat.ts fix.
