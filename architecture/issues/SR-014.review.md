# SR-014: Remove Old Colyseus, Custom Networking, and Client Authority

## Objective

Cleanly remove obsolete networking and client-side authoritative gameplay once the vecs migration is functional and tested.

## Scope

- Delete Colyseus `UniverseRoom` and tests.
- Delete `apps/client/src/network/ecsMirror.ts`.
- Delete custom `packages/common/src/network/protocol.ts` and old snapshot/delta helpers.
- Remove Colyseus dependencies from package files and lockfile.
- Remove browser-side calls to `initGame()` and gameplay system imports.
- Remove or relocate client-side factories and systems that are now server-owned.
- Update README or developer docs with new server/client startup flow.

## Dependencies

- `SR-003`
- `SR-004`
- `SR-010`
- `SR-011`
- `SR-013`

## Blocks

- Final release of the migration.

## Implementation Notes

- Do this late. Premature deletion makes parallel migration harder.
- Keep client render systems and client-only particles.
- Verify no import path still references Colyseus or custom network protocol.
- Verify package lock no longer contains Colyseus packages.

## Acceptance Criteria

- No `colyseus` or `colyseus.js` dependency remains.
- No Colyseus imports remain.
- No custom ECS snapshot/delta protocol remains.
- Client does not instantiate authoritative gameplay entities.
- Server is the only authority for gameplay state.
- Tests, typecheck, lint, and manual multiplayer smoke test pass.

## Review

**Verdict:** approved

**Verification gates:**
- typecheck: pass (all 3 workspaces compile with `--noEmit`, zero errors)
- lint: pass (all 3 workspaces pass eslint, zero warnings)
- test: pass (client 70/70, server 24/24, common 4/4 — 98 tests total)
- build: pass (client Vite production build succeeds, server/common compile)

**Must-fix:**
- (none)

**Nits (optional):**
- `apps/client/src/components/` contains 20+ barrel re-export files for gameplay components (Alien.ts, Asteroid.ts, Bullet.ts, Collider.ts, ShipInput.ts, Rocket.ts, Boomerang.ts, etc.) that are no longer imported or registered in the client world. These are dead code after the gameplay system removals. Consider deleting them in a follow-up hygiene pass to keep `components/index.ts` trimmed to only the components the client world actually registers.
- `apps/client/src/components/index.ts` still re-exports gameplay components from `@spacerocks/common` via `export { ... } from '@spacerocks/common'` for all the removed gameplay types. The file compiles and tests pass since no code imports these stubs anymore, but it signals stale ownership to future readers.

**Notes:**
- Every acceptance criterion is satisfied: no colyseus/colyseus.js dependency remains in package.json or lockfile (0 matches in grep), Colyseus `UniverseRoom` and its test are deleted, `ecsMirror.ts` and `protocol.ts` are deleted, `initGame()` is gone from `main.ts`, all 6 client factories (Ship, Asteroid, Alien, Pickup, Bullet, Rocket, Boomerang) and all 16 client gameplay systems are removed, and the README now documents `npm run dev:server` for the authoritative vecs server.
- Render systems (Render, NetworkRender, UI, all draw/* systems), stars, local particles (DecaySystem, Particle factory), DOM/canvas setup, and keyboard input remain functional in the client.
- 16 client test files pass, including the DecaySystem test which was correctly updated to use Particle/Position/Velocity components matching the renamed `LocalParticleSystem`.
- The `packages/common/src/index.ts` barrel no longer exports protocol/snapshot/delta types — the custom ECS network protocol has been cleanly excised.
- Lockfile size dropped by 1406 lines. No colyseus packages remain in `node_modules` or lockfile entries.

VERDICT: approved
