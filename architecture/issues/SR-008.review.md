# SR-008: Move Spawning, Waves, Pickups, Aliens, and RNG to Server

## Objective

Move all authoritative spawning and timed game progression to the server.

## Scope

- Migrate asteroid spawning and splitting support.
- Migrate wave progression.
- Migrate alien spawning.
- Migrate pickup spawning and pickup metadata.
- Replace `RandomClock.effectFunc` with server-owned timed spawn systems.
- Replace gameplay `Math.random()` usage with server-side RNG utilities.

## Dependencies

- `SR-003`
- `SR-005`

## Blocks

- `SR-010`

## Systems and Factories to Move

- `game.ts` initialization logic, server equivalent only.
- `Wave`
- `RandomClockSystem`
- `createAsteroid`
- `createAlien`
- `createPickup`

## Implementation Notes

- Spawns use fixed world dimensions, not browser canvas dimensions.
- Random asteroid shapes are server-generated and synced through `Shape` or a compact `AsteroidView` representation.
- Pickup effects are enum-driven and resolved on server collision.
- Spawn timers should pause or no-op when the game state is not playing.

## Acceptance Criteria

- Server initializes the game world.
- Server progresses waves when asteroids/aliens are cleared.
- Server spawns pickups and aliens over time.
- Clients render spawned entities without local spawn code.

## Review

**Verdict:** approved

**Verification gates:**
- typecheck: pass (all three workspaces — client, server, common — compile cleanly)
- lint: pass (zero warnings across all workspaces)
- test: pass (31 client files / 175 tests, 5 server files / 15 tests, 1 common file / 4 tests — all green)
- build: n/a (ticket does not touch build wiring)

**Must-fix:**
- (none)

**Nits (optional):**
- `isPlaying()` in `spawning.ts:367-370` returns true when `GameStateView` entity is absent. This matches the comment "Spawning timers should pause or no-op when the game state is not playing" but the `!gameState` guard is arguably inverted (missing game state = playing). This is harmless in practice because `initializeGameWorld` always creates the entity on startup, but a future reviewer may want to reconcile the semantics.
- The `SpawnTimer` class (spawning.ts:64-69) duplicates fields that also exist on `RandomClock` in `GameplayComponents.ts:128-137` (both have `kind`, `minWait`, `maxWait`, `nextTick`). Consider whether these could share a common interface instead of defining a parallel class.

**Notes:**
- Asteroid splitting support is correctly scoped: `createAsteroid` accepts `level: 1|2|3` so that SR-010 collision handlers can create sub-asteroids. The actual split-on-destroy logic remains client-side in Collision.ts/LaserSystem.ts, which is owned by SR-010.
- `Asteroid` and `Decay` are server-only components (not in NETWORK_COMPONENTS), while `AsteroidView`, `PickupView`, `GameStateView` are in NETWORK_COMPONENTS. This is the correct dual-layer pattern from the plan.
- No client files were touched; no packages files were touched. The diff is contained entirely within `apps/server`.
- Test strategy uses `World` (base vecs) + a mock of `@vworlds/vecs-server` to test server systems without a live network — a pragmatic and clean approach.
