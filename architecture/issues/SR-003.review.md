# SR-003: Build Vecs Server Shell and Connection Lifecycle

## Objective

Replace the Colyseus server shell with a vecs-server shell that accepts WebRTC client connections and syncs at least one server-created entity.

## Scope

- Replace `apps/server/src/index.ts` Colyseus startup with Express + `VecsListener`.
- Create a `ServerWorld` named `main` using `NETWORK_COMPONENTS`.
- Add simulation, collect, and send phases.
- Install vecs server systems with `server.installSystems({ collectPhase, sendPhase })`.
- Start a fixed-rate simulation loop.
- Configure `View` for clients so they can see networked entities.
- Add a temporary server-created debug entity to verify sync.

## Dependencies

- `SR-001`
- `SR-002`

## Blocks

- `SR-006`
- `SR-008`
- `SR-014`

## Implementation Notes

- The vecs demo uses `VecsListener.registerWorld(world)` and `vecsListener.listen(app, { ordered: false, maxRetransmits: 2 })`.
- Default world path is `/rtc/v1/world/:worldName`.
- Set `View.dsl = true` for initial implementation. Interest management can come later.
- Keep logging simple and preserve current `PORT` env behavior where possible.

## Acceptance Criteria

- Server starts without Colyseus.
- A `ServerWorld` progresses at a fixed rate.
- Connected clients receive networked entities when a matching client shell exists.
- Existing Colyseus `UniverseRoom` is no longer part of server startup.

## Review

**Verdict:** approved

**Verification gates:**
- typecheck: pass (all three workspaces — client, server, common — compile cleanly with `tsc --noEmit`)
- lint: pass (all three workspaces — zero warnings/errors via eslint)
- test: pass (all 181 tests pass: 175 client, 2 server, 4 common)
- build: n/a (ticket does not touch build wiring per review checklist)

**Must-fix:**
- (none)

**Nits (optional):**
- (none)

**Notes:**
- `startServer` return type changed from `{ gameServer, port }` to `{ app, server, world, vecsListener, tick, simulationPhase, port }`. The only caller is the `import.meta.url` block in the same file, which destructures only `{ port }`, so this is safe. No external consumers.
- The old Colyseus `UniverseRoom` class in `src/rooms/UniverseRoom.ts` and its test `tests/UniverseRoom.test.ts` remain in the tree but are no longer imported by `index.ts`. These are SR-014 cleanups and correctly left untouched by this ticket.
- The acceptance criterion "Connected clients receive networked entities when a matching client shell exists" is met on the server side — the server creates a `Networked` debug entity and configures `View.dsl = true`. The client shell (SR-004) implements the matching receiver. No action needed.
- `colyseus` remains in `package.json` dependencies (SR-014 removal); no new Colyseus usage introduced.
