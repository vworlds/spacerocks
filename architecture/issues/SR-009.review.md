# SR-009: Move Shooting and Weapon Systems to Server

## Objective

Move all weapon firing, cooldowns, ammo, projectile creation, and projectile movement behavior to the server.

## Scope

- Migrate default bullets.
- Migrate aura weapon firing.
- Migrate laser weapon state.
- Migrate rocket creation and homing behavior.
- Migrate boomerang creation, return behavior, arming, and catch readiness.
- Attach owned projectiles to the owning ship/session through relationships.

## Dependencies

- `SR-005`
- `SR-006`
- `SR-007`

## Blocks

- `SR-010`

## Systems and Factories to Move

- `Shooting`
- `LaserSystem`
- `RocketSystem`
- `BoomerangSystem`
- `createBullet`
- `createRocket`
- `createBoomerang`

## Implementation Notes

- The client input payload has a semantic `shoot` boolean only.
- Cooldown is server-owned.
- Weapon ammo/state is server-owned and optionally mirrored through `WeaponView`.
- Projectiles should be `Networked` if they need to render directly.
- Owned projectiles should be children of the owner ship or player parent for disconnect cleanup.

## Acceptance Criteria

- Server creates bullets/rockets/boomerangs from player input.
- Weapon cooldown and ammo cannot be bypassed by client mutation.
- Clients render projectiles from server network state.
- Disconnect destroys active owned projectiles.

## Review

**Verdict:** approved

**Verification gates:**
- typecheck: pass (all workspaces: client, server, common — no errors)
- lint: pass (all workspaces — no warnings)
- test: pass (197 tests total: client 175, server 18, common 4 — all passing, including 3 new shooting.test.ts tests)
- build: n/a (this ticket does not touch build wiring)

**Must-fix:**
- (none)

**Nits (optional):**
- `Decay` component is registered in both `spawning.ts:79` and `shooting.ts:63`. vecs is idempotent by component class so this is harmless, but consolidating to one registration site would be cleaner. (Not a must-fix.)

**Notes:**
- All acceptance criteria verified: bullets, rockets, and boomerangs are created server-side from `PlayerInputIntent.shoot`; cooldown (`ShootingCooldown`) is server-owned and enforced; weapon state (`AuraWeapon`, `LaserWeapon`, `RocketWeapon`, `BoomerangWeapon`) is server-authoritative and mirrored via `WeaponView` with `entity.modified()` calls; projectiles use `ChildOf` for ownership-based disconnect cleanup (confirmed by boomerang test: ship destroy cascades to child); all projectiles are `Networked` so clients will render from server state.
- `Boomerang.ownerId` stores a raw `eid` (number), not an `Entity` reference — safe for serialization even though `Boomerang` is server-only (not in `NETWORK_COMPONENTS`).
- `NETWORK_COMPONENTS` ordering is untouched; `Position` remains at index 0.
- No new colyseus usage, no client mutations, no lockfile changes.
- EM dual-running policy: client-side shooting code is intentionally kept per SR-014 removal plan — this review does not flag it. All work is additive server-side code.
